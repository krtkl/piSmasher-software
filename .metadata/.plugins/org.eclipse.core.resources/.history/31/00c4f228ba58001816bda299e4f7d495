




#include <stdio.h>
#include <err.h>
#include <pthread.h>


void *thread_entry(void *ptr)
{


	return NULL;
}




int main(int argc, char *argv[])
{
	pthread_t vdma_thread;
	int  error, thread_var;

	error = pthread_create(&vdma_thread, NULL, thread_entry, &thread_var);
	if (error != 0)
		err(error, "creating VDMA thread");


	/* Map the register space for the VDMA */



	/* Initialize the device */


	/* Reset the DMA */

	/* Enable interrupts */



	error = pthread_join(vdma_thread, NULL);
	if (error != 0)
		err(error, "joining terminated thread");

	return 0;
}
